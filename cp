The cp command copies the contents of one file (first parameter) to the second file (second parameter).If the second file does not exist, then it will create that file. Alternatively, cp command can copy file (specified by the first parameter) to the path (specified by second parameter).

